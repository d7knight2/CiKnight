# GitHub App Configuration
GITHUB_APP_ID=your_app_id
GITHUB_PRIVATE_KEY="your_private_key"
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# Server Configuration
# PORT: The port the application will listen on
#       - Local development: Use 3000 (default if not set)
#       - Cloud Run: Automatically set to 8080 by Cloud Run
#       - Docker: Set via -e PORT=<port> when running container
PORT=3000
NODE_ENV=production

# Optional: Specific installation ID if you want to test with a specific installation
# GITHUB_INSTALLATION_ID=your_installation_id
