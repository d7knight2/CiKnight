# GitHub App Configuration
GITHUB_APP_ID=your_app_id
GITHUB_PRIVATE_KEY="your_private_key"
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# Server Configuration
# PORT: The port the application will listen on
#       - Local development: Use 3000 (default if not set)
#       - Cloud Run: Automatically set to 8080 by Cloud Run
#       - Docker: Set via -e PORT=<port> when running container
PORT=3000
NODE_ENV=production

# Webhook Security Configuration
# WEBHOOK_IP_RESTRICTION_ENABLED: Enable/disable IP address validation
#   - Set to 'false', '0', 'no', or 'off' to disable IP restrictions
#   - Any other value (including undefined) enables IP restrictions (default: enabled)
#   - When enabled, only requests from GitHub's webhook IP ranges are accepted
WEBHOOK_IP_RESTRICTION_ENABLED=true

# WEBHOOK_IP_FAIL_OPEN: Behavior when IP validation fails due to errors
#   - Set to 'true' to allow requests when validation fails (fail-open mode)
#   - Set to 'false' to reject requests when validation fails (fail-closed mode, default)
#   - Only applies when WEBHOOK_IP_RESTRICTION_ENABLED=true
WEBHOOK_IP_FAIL_OPEN=false

# TRUST_PROXY: Trust proxy headers for IP extraction
#   - Set to 'true' to trust X-Forwarded-For and X-Real-IP headers
#   - Set to 'false' (default) to only use socket remote address
#   - ⚠️  IMPORTANT: Set to 'true' when running behind a reverse proxy or load balancer
#     (e.g., Google Cloud Run, AWS ALB, nginx, Cloudflare) to correctly extract client IPs
#   - When running behind a proxy, the socket IP will be an internal IP (like 169.254.x.x)
#     and the actual GitHub IP will be in the X-Forwarded-For header
#   - Only enable if behind a trusted reverse proxy to prevent header spoofing attacks
TRUST_PROXY=false

# Optional: Specific installation ID if you want to test with a specific installation
# GITHUB_INSTALLATION_ID=your_installation_id
